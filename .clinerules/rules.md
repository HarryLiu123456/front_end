1. reference文件夹中是一个参考项目，除了reference文件夹以外的部分是当前项目、目前项目、我的项目
2. 与python有关的命令都需要在前面加个uv run才能正常运行
3. 所有新增、修改的代码都需要加入注释
4. 使用如下命令'uv run uvicorn server:app --reload'可以开启当前网页
5. 我当前使用的终端是windows的powershell7，请使用powershell7中支持的命令